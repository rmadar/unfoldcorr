from setuptools import setup

setup(name='umodel',
      version='0.1',
      description='Unfolding model with correlations between bins',
      packages=['umodel'],
      author_email='romain.madar@cern.ch',
      author='Romain Madar',
)
